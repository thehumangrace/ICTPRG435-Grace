#!/usr/bin/env python3

# Coded Python Script
# Author XXXX     Date XX-XX-XXXX
importos

# import operating system library functions
#iinitialize variables
hours=0
rate=0
pay=0

os.system("clear)
# run the Linux system clear screen command
# input values
print("Pay is $ : " + str(pay))
hours=float(input("Enter your hours: ") # input() allows users to enter strings
rate=float(input("Enter pay rate: ));;;;; # float() coverts strings to decimals
#Calculate pay as float (decimal)
pay=hours*rate